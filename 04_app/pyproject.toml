[project]
name = "attach-war-room"
version = "0.1.0"
description = "bolttech Attach War-Room — Databricks App (FastAPI + React)"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "psycopg2-binary",
    "openai",
    "databricks-sdk>=0.114.0",
    "pydantic",
]
